requests>=2.31.0
beautifulsoup4>=4.12.0
pillow>=10.0.0
lxml>=4.9.0
imagehash>=4.3.0

# LaMetric AI Icon Selector dependencies
anthropic>=0.52.0        # Claude API client (claude-opus-4-6 + structured outputs)
pydantic>=2.0.0          # Structured output schema validation

# AppDaemon (only required when running as an AppDaemon app)
# Uncomment if not already installed in your AppDaemon environment:
# appdaemon>=4.4.0
